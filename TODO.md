- Allow multiple sandboxes to run at the same time, without docker name or host port conflicts

- Streamline anthropic login process by opening browser on the host and supporting the oauth workflow. Save credentials if possible so new sandboxes don't need to login again.

- Presets for permissions (allow all read, bash)

- support cut/past in vnc viewer

- support audio in vnc viewer

- auto connect to vnc when loading the page